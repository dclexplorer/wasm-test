# wasm-test

Required installs (global)
```bash
cargo install wasm-pack
cargo install simple-http-server
```

build and run:
```bash
make dev # build all in dev

make run # build all in dev + serve

make serve # just serve a http server

make release # build all in release
```